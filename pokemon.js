axios.get("https://fizal.me/pokeapi/api/v2/id/257.json")
	.then(response => {
    HP = response.data.id.
}


class pokemon {
  constructor(HP, ATK, DEF, ABIL) {
  this.HP = HP;
  this.ATK = ATK;
  this.DEF = DEF;
  this.ABIL = ABIL;
  } 

let blaziken = {
  HP: this.HP,
  ATK: this.ATK,
  DEF: this.DEF,
  ABIL: this.ABIL
};

let spoink = {
  HP: this.HP,
  ATK: this.ATK,
  DEF: this.DEF,
  ABIL: this.ABIL
};

let dragonair = {
  HP: this.HP,
  ATK: this.ATK,
  DEF: this.DEF,
  ABIL: this.ABIL
};
